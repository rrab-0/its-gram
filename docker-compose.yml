version: "3.1"

services:
    postgres:
        image: postgres
        restart: always
        environment:
            POSTGRES_PASSWORD: ${DB_PASSWORD}
            POSTGRES_DB: ${DB_NAME}
        ports:
            - 5433:${DB_PORT}

    api:
        build: .
        restart: always
        ports:
            - ${DEV_PORT}:${DEV_PORT}
        depends_on:
            - postgres
